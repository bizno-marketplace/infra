# Copy this file to .env.secrets (already in .gitignore, never committed)
# and fill in real values. create-secrets.sh reads from .env.secrets.

POSTGRES_DB=bizno_auth
POSTGRES_USER=bizno
POSTGRES_PASSWORD=change-this-password

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-this-password

REDIS_PASSWORD=change-this-password
